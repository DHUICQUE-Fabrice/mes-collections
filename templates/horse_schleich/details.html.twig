{% extends 'base.html.twig' %}
{% block title  %}
    {{ parent() }} | {{ schleich.name }}
{% endblock %}
{% block body %}
    <div class="card-flyer">
        <div class="text-box">
            <div class="image-box">
                <img src="{{ schleich.picture }}" alt="{{ schleich.name }}" />
            </div>
            <div class="text-container">
                <h6>{{ schleich.name }}</h6>
                <p>{{ schleich.biography }}</p>
            </div>
        </div>
    </div>
{% endblock %}

